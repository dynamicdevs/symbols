import { Component, h, Prop, Host } from '@stencil/core';
import { StyleType } from '../../../types';

import svgSolidIcon from '../../../assets/icons/solid/ic_chick.svg';
import svgOutlineIcon from '../../../assets/icons/outline/ic_chick.svg';

/** @internal **/
@Component({
  tag: 'ic-chick-svg',
  shadow: false,
})
export class IconChickSvg {
  @Prop() iconClass?: string;
  @Prop() type: StyleType = 'solid';

  render() {
    return (
      <Host
        class={`${this.iconClass} symbol-icon-container`}
        innerHTML={this.type === 'solid' ? svgSolidIcon : svgOutlineIcon}
      ></Host>
    );
  }
}
